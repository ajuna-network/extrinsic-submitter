# Extrinsics Submitter

To run:

- [Install `yarn`](https://classic.yarnpkg.com/lang/en/docs/install)
- Install dependencies:
  ```
  yarn
  ```
- Run the `main` script:
  ```bash
  yarn run main
  ```
